'use strict';

/**
 * publication-slider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::publication-slider.publication-slider');
