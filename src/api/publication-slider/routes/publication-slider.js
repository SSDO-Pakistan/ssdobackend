'use strict';

/**
 * publication-slider router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::publication-slider.publication-slider');
